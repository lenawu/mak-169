// %h1 Contact Administrators

// = form_tag contact_path do
    
//     = label :contact, :name, 'Name'
//     = text_field :contact, 'name'
    
//     = label :contact, :email, 'Email'
//     = text_field :contact, :email, 'email'
    
//     = label :contact, :comments, 'Comments'
//     = text_field :contact, :comments, 'comments'
    
//     = submit_tag 'Submission'
    
.container
  %h1 Contact
  = simple_form_for @contact, :html => {:class => 'form-horizontal' } do |f|
    = f.input :name, :required => true
    = f.input :email, :required => true
    = f.input :message, :as => :text, :required => false, :input_html => {:rows => 10}

    .hidden
      = f.input :nickname, :hint => 'Leave this field blank!'
    .form-actions
      = f.button :submit, 'Send message', :class=> "btn btn-primary"